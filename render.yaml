services:
  - type: web
    name: incubator-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn incubator.wsgi
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: jeb
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"

databases:
  - name: jeb
    plan: free
